* php module
* auth module
* use server->chunksize every where and add configuration
* connection code break reception part from emission part for pipelining
* http2 module
* KeepAlive only for HTTP/1.0 and over
* pipelining only for HTTP/1.1 and over
* date and expired file module
