if [ "$PYTHON" != "y" ]; then
	echo "Python module disabled"
	DISABLED=1
fi
if [ "$TESTDIR" != "./tests/" ]; then
	echo "test must run from SRC directory"
	DISABLED=1
fi
DESC="test a request on Python script"
CONFIG=test3.conf
TESTCODE=200
