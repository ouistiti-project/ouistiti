if [ "$AUTHZ_JWT_CHECKHEADER" != "y" ]; then
	echo "JWT header checking disabled"
	DISABLED=1
fi
DESC="Auth Bearer bad header of JWT"
CONFIG=test13.conf
TESTCODE=302
TESTRESPONSE=error302_redirect_rs.txt
